model: {
  framework: onnx,
  name: yolop,
  save_dir: ./output,
  weight: ./yolop.onnx,
  inputs: [
    {
      name: images,
      shape: [1, 3, 384, 640],
      layout: NCHW,
      format: RGB,
      image: {
        format: YUV422,
        size: ,
        crop: ,
      },
      dtype: uint8,
      mean: [ 0.485, 0.456, 0.406 ],
      std: [ 0.229, 0.224, 0.225 ],
      data_path: ../../data/pic/lane.jpg,
    }
  ],
  impl_class: YoloP,
}

quant: {
  ptq_cfg_path: ,
  calib_dir: ../../data/datasets/bdd100k/images/100k/val,
  calib_num: 30,
  debug_level: 1,
  calib_method: ,
}

build: {
  target: H30,
  opt_level: 0,
  input_layout: ,
  output_layout: NHWC,
}

demo: {
  data_dir: ../../data/datasets/bdd100k/images/100k/val,
  test_num: 10,
}

perf: {
  data_dir: ,
  test_num: 100,
  mode: throughput,
  end2end: ,
}

accuracy: {
  data_dir: ../../data/datasets/bdd100k/images/100k/val,
  test_num: 1,
  dataset_class: Dataset,
}


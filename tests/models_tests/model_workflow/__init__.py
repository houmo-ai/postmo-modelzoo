"""Reusable contracts and infrastructure shared by model-test flows.

Modules in this package deliberately avoid pytest-specific behavior; they
provide configuration, command, workspace, cache, artifact, and validation
primitives for the handlers in :mod:`tests.models_tests.test_flows`.
"""

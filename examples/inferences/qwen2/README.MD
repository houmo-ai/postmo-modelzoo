# Qwen2(通义千问2)

本示例展示如何把编译后的Qwen2模型部署到后摩鸿途芯片的设备上。

[TOC]

## 1 模型说明

本例使用的 Qwen2 实现来源于[https://huggingface.co/Qwen/Qwen2-7B-Instruct](https://huggingface.co/Qwen/Qwen2-7B-Instruct)。

目前芯片模型只能在linux环境下量化和编译，请参考houmo-modelzoo/models/llm/qwen2中的量化和编译。

## 2 快速开始

目前Qwen模型仅提供python脚本部署方式。

1. 将python目录加入PYTHONPATH环境变量

2. 设置运行库路径
    - linux环境将lib目录加入LD_LIBRARY_PATH环境变量
    - windows环境将bin目录下dll库以及houmo_drv包bin目录下的cyghm800_hal.dll拷贝到python/tcim_lite目录下

3. 将编译并转换好的模型文件拷贝到当前目录，包括6个文件：
    qwen2_prefill_part1.hmm
    qwen2_prefill_part2.hmm
    qwen2_prefill_head.hmm
    qwen2_decode_part1.hmm
    qwen2_decode_part2.hmm
    qwen2_decode_head.hmm

4. 将quant_embedding.pth文件拷贝到当前目录，该文件存在于量化模型目录下

5. 运行demo.py

## 3 参考结果

### 3.1 演示结果

```bash
question:
请介绍一下存算一体技术的优势
response:
存算一体（Co-Processing）技术是一种将计算（Compute）和存储（Storage）功能整合在同一设备或系统中的技术。这种技术的优势主要体现在以下几个方面：

1. **性能提升**：存算一体可以显著减少数据在存储和计算之间传输的时间，从而提高整体处理速度。数据不需要在存储和处理之间来回传输，减少了延迟，提高了数据处理的效率。

2. **低延迟**：由于数据处理和存储在同一设备上进行，可以大大减少数据访问的延迟，特别是在需要频繁访问数据的场景下，如实时分析、流式计算等。

3. **高带宽**：存算一体技术可以提供更高的数据带宽，使得数据处理过程中的数据传输更加流畅，能够支持更大数据量的处理。

4. **成本效益**：通过将存储和计算功能整合，可以减少硬件需求，从而降低总体拥有成本（TCO）。同时，这种设计可以更灵活地调整资源使用，根据实际需求动态分配计算和存储资源。

5. **能源效率**：存算一体设计可以更有效地利用能源，因为减少了数据传输过程中的能耗。特别是在大规模数据中心和云计算环境中，能源效率的提升对于降低成本和减少环境影响至关重要。

6. **安全性增强**：在存算一体的系统中，数据在存储和处理之间传输的次数减少，降低了数据泄露的风险。此外，可以更方便地实施数据加密和访问控制策略。

7. **灵活性和可扩展性**：存算一体架构允许更灵活地扩展计算和存储资源，可以根据应用需求动态调整，提供更好的性能和资源利用效率。

8. **简化系统架构**：通过将存储和计算功能整合，可以简化系统架构，减少复杂性，使得系统设计和维护更加容易。

存算一体技术的应用范围广泛，包括但不限于数据中心、云计算、大数据处理、人工智能、高性能计算等领域，能够显著提升应用的性能和效率。
```

### 3.2 性能结果

```bash
total: 408 tokens, cost 51.966 s
prefill time: 859.993 ms, 1.16 tokens/s
decode average time: 125.565 ms, 7.96 tokens/s
end2end average time: 127.367 ms, 7.85 tokens/s
```

## 4 免责声明

您明确了解并同意，以下链接中的软件、数据或者模型由第三方提供并负责维护。在以下链接中出现的任何第三方的名称、商标、标识、产品或服务并不构成明示或暗示与该第三方或其软件、数据或模型的相关背书、担保或推荐行为。您进一步了解并同意，使用任何第三方软件、数据或者模型，包括您提供的任何信息或个人数据（不论是有意或无意地），应受相关使用条款、许可协议、隐私政策或其他此类协议的约束。因此，使用链接中的软件、数据或者模型可能导致的所有风险将由您自行承担。
